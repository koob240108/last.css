input.input, select.input
  outline: none
  background-color: transparent
  box-sizing: border-box
  display: block
  width: 100%
  color: inherit

  border-radius()

  border: 1px solid fc(.1)
  height: 2.5em
  font-size: fs()
  padding: 0 .75em
  &:focus
    border-color: fc(.3)
